default: --no-source --color --format pretty --format html --out report.html -p prd

#Enviroment
hml: ENVIRONMENT="hml"
prd: ENVIRONMENT="prd"

#Browsers
chrome: BROWSER="chrome"

#Visual mode
headless: HEADLESS="true"

